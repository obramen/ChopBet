<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="antrix.chopbet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activities.activityChopBet"
            android:label="@string/app_name">
        </activity>
        <activity android:name=".Activities.activityEditUserProfile"
            android:windowSoftInputMode="adjustResize">

        </activity>
        <activity android:name=".Activities.activityUserProfile">

        </activity>
        <activity android:name=".Activities.activityRegisterLogin"
            android:windowSoftInputMode="adjustResize">

        </activity>

        <activity android:name=".Activities.activityPhoneVerify"
            android:windowSoftInputMode="adjustResize"/>
        <activity android:name=".Activities.activityBetDetails"/>
        <activity android:name=".Activities.activityEditUsername"/>
        <activity android:name=".Activities.activitySplashScreen">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".Activities.activityAddFriend"/>
        <activity android:name=".Activities.activityFriendRequests"/>


    </application>

</manifest>